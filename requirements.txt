# ============================
# Dependencies for the Agent
# ============================
# LiteLLM — unified interface to call 100+ LLM APIs (DeepSeek, OpenAI, etc.)
litellm>=1.60.0
# psycopg2-binary — PostgreSQL driver for connecting to CockroachDB
psycopg2-binary>=2.9.9
# python-dotenv — load secrets from .env file
python-dotenv>=1.0.0
# structlog — structured logging
structlog>=24.0.0
# sentry-sdk — error tracking and performance monitoring
sentry-sdk[stdlib]>=2.0.0
# tenacity — retry logic with exponential backoff
tenacity>=8.0.0
# pydantic-settings — type-safe configuration with validation
pydantic-settings>=2.0.0
# pytest — testing framework
pytest>=8.0.0
# fastapi — REST API for TUI backend
fastapi>=0.115.0
# uvicorn — ASGI server for FastAPI
uvicorn>=0.34.0
